<p id="notice"><%= notice %></p>

<h2><%= @user.name %></h2>
<div>
	<section>
		<%= render 'shared/stats' %>
	</section>
</div>
<div>
	<%= render 'follow_form' if session[:user_id] %>
</div>

<% @user.posts.last(25).reverse.each do |post| %>
  <div class="post">
    <p><%= image_tag(post.url) %></p>
    <p style="font-weight: bold;"><%= post.content %></p>
  </div>
<% end %>