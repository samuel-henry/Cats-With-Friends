<% unless session[:user_id] == @user.id %>
	<div id="follow_form"?
		<% if User.find_by_id(session[:user_id]).following?(@user) %>
			<%= render 'unfollow' %>
		<% else %>
			<%= render 'follow' %>
		<% end %>
	</div>
<% end %>
